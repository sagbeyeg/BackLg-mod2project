
<div class="home_title">
  <h1>B@ckLg is a Video Game Cataloging App</h1>
<div>

<div class="home_description">
  <h3>
    With access to over 400,000 thousand games,
    B@ckLg is your one stop shop for your game cataloging needs
  </h3>
</div>

<div class="container my-4">
    <div class="row">
        <% Game.all.sample(3).each do |game|%>
            <div class="col-md-4">
                <div class="card shadow">
                    <img src= <%= game.image_url %> class="card-img-top">
                    <div class="card-body">
                        <h5 class="text-center" class="card-text"><%= link_to "#{game.name}", game_path(game) %></h5>
                        <br>
                        <li class="card-text"><strong>Rating:</strong> <%= game.rating %></li>
                        <li class="card-text"><strong>Genre:</strong> <%= game.genre %></li>
                        <li class="card-text"><strong>Number of Ratings:</strong> <%= game.rating_count %></li>
                        <br>
                    </div>
                </div>
            </div>
        <% end%>
    </div>
</div>
<br><br>
  <div class="home_button">
    <% if @current_user != nil %>
      <%= button_to "View More Games", games_path, method: :get%>
    <% else %>
      <h3>What are you waiting for?</h3>
      <%= button_to "Signup", new_user_path, method: :get %>
      <% end %>
  <div>